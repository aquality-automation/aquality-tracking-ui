import { by, element } from 'protractor';

export const baseUrl = `/**`;

export const elements = {
    uniqueElement: element(by.id('not-found-page'))
};

export const names = {
    pageName: '404 Page'
};
